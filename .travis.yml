sudo: required

services:
  - docker

language: python

python:
  - "2.7"

install:
  - pip install -r requirements.txt

script:
  - ./fetch/owncloud/fetch.sh
  - ./runtests_server.sh test travis 2.1-3p1 -t travis
